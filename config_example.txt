# JohnnyBot Configuration
# Copy this file to config.txt and edit the values for your deployment:
#   cp config_example.txt config.txt
# config.txt is gitignored, so your customizations won't conflict with git pull.

[general]
# Timezone for scheduled jobs and event announcements
BOT_TIMEZONE = America/Chicago

# The IP of the interface which will host downloadable archives
HOST_IP = 0.0.0.0

[channels]
MODERATORS_CHANNEL_NAME = moderators_only
# Comma-separated list of protected channel names
PROTECTED_CHANNELS = 🫠・code_of_conduct, 🧚・hey_listen, 👯・local_events

[roles]
MODERATOR_ROLE_NAME = Moderators
# Comma-separated lists for the Voice Chaperone function
ADULT_ROLE_NAMES = Dads, GrownUps
CHILD_ROLE_NAMES = Kids, Bambinos, Girls

[features]
VOICE_CHAPERONE_ENABLED = true
UPDATE_CHECKING_ENABLED = true
UPDATE_CHECK_REPO_URL = https://github.com/BurbSec/JohnnyBot
